import torch.nn.functional as F
from torch import nn
import torch

device = 'cuda' if torch.cuda.is_available() else 'cpu'

raise NotImplementedError("Please implement the Transformer model in src/models/transformer.py")
